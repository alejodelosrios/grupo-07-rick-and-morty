import React, { useState } from "react";
import CharactersContainer from "../components/CharactersContainer";

const Home = () => {
  return (
    <div>
      <CharactersContainer />
    </div>
  );
};

export default Home;
